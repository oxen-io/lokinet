variables:
  GIT_STRATEGY: fetch

stages:
  - build
  - test

build:linux:
  image: alpine:latest
  tags:
    - linux
  stage: build
  before_script:
    - apk add --update g++ make cmake automake libtool autoconf linux-headers libsodium-dev ninja
  script:
    - make 
  artifacts:
    paths:
      - "lokinet"

